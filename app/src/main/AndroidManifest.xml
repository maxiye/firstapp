<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.maxiye.first">

    <uses-sdk android:maxSdkVersion="27"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.NFC"/>
    <!-- 添加快捷方式 -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT"/>
    <!-- 移除快捷方式 -->
    <uses-permission android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT"/>
    <!-- 查询快捷方式 -->
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false"/>

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/ic_fingerprint_orange_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_fingerprint_orange_24dp"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.maxiye.first.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"/>
        </provider>

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/setting"
            android:logo="@drawable/ic_settings_black_24dp"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".TestActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/test"
            android:logo="@drawable/ic_broken_image_black_24dp"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <data
                    android:host="8office.cn"
                    android:pathPrefix="/.*"
                    android:scheme="http"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:mimeType="image/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>

                <category android:name="android.intent.category.DEFAULT"/>

                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

        <receiver android:name=".MyReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".WebViewActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/webview"
            android:logo="@drawable/ic_public_orange_60dp">
        </activity>
        <activity
            android:name=".GifActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/get_gif"
            android:logo="@drawable/ic_image_black_24dp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:parentActivityName=".TestActivity"
            android:name=".api.ExchangeRateActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/exchange_rate"
            android:logo="@drawable/ic_attach_money_black_24dp">
        </activity>
        <activity
            android:parentActivityName=".TestActivity"
            android:name=".api.WeatherActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/weather"
            android:logo="@drawable/ic_cloud_queue_black_24dp">
        </activity>
        <activity android:name=".api.IPAddressActivity"  android:parentActivityName=".TestActivity" android:label="@string/ip_address"/>
        <activity android:name=".api.PhoneAddressActivity"  android:parentActivityName=".TestActivity" android:label="@string/phone_address"/>
        <activity android:name=".api.IDAddressActivity"  android:parentActivityName=".TestActivity" android:label="@string/id_address"/>
        <activity android:name=".api.PostcodeActivity"  android:parentActivityName=".TestActivity" android:label="@string/postcode"/>
        <activity android:name=".api.BJTimeActivity"  android:parentActivityName=".TestActivity" android:label="@string/bj_time"/>
        <activity android:name=".api.WorkdayActivity"  android:parentActivityName=".TestActivity" android:label="@string/workday"/>
    </application>

</manifest>