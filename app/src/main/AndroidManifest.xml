<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxiye.first">
    <uses-sdk android:minSdkVersion="21" android:maxSdkVersion="25" android:targetSdkVersion="25"/>

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_fingerprint_orange_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_fingerprint_orange_24dp"
        android:supportsRtl="true"
        android:theme="@style/CustomActBarThe">
        <activity android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <activity android:name=".SettingActivity"
            android:parentActivityName=".MainActivity"
            android:logo="@drawable/ic_settings_black_24dp"
            android:label="@string/title_setting_activity">
        </activity>
    </application>

</manifest>